# TODO

- [ ] update mage debug target to print new default log path
- [ ] remove timestamp from log file, overwrite each run
- [ ] clickable icon button for address bar action item
- [ ] validate address bar text
- [ ] rewrite request functions for tree widget
- [ ] isolate creation and binding of various UI elements
- [x] maximize window without using fullscreen
  - can't do, fyne doesn't support a max size due to design decisions
- [ ] LOGGING: replace default path use (points at default YAML file)
- [ ] define theme(s)
- [ ] MODEL: current structs don't support nested groups
  - necessary to load request tree
