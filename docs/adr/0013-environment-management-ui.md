# Environment Management UI

We decided to implement a dedicated interface for managing Environment variables:
1. Environment editing is separated from the primary workspace and accessed via a modal **Environment Manager** dialog.
2. The modal displays a list of existing environment profiles on the left with "Add" and "Delete" buttons.
3. The right side of the modal allows the user to rename the selected environment and edit its variables using a **Dynamic Row List**.
